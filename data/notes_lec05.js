const notesLec05 = [
  {
    "title": "مقدمة عن المؤقتات Timers في متحكم 8051",
    "conceptOverview": "المؤقت (Timer) هو جزء أساسي في أي متحكم دقيق، ويُستخدم إما كمؤقت زمني (Timer) أو كعداد للأحداث (Counter). في التطبيقات العادية كنا نستخدم الـ Delay Code عن طريق حلقات for لإضاعة وقت المعالج، ولكن هذا الأسلوب غير دقيق ويستهلك موارد المعالج. باستخدام الـ Timers يمكننا ضبط الوقت بشكل أدق وبدون إهدار موارد المعالج. يحتوي 8051 على مؤقتين: Timer 0 و Timer 1، وكل منهما 16-بت، مقسمين إلى مسجلين 8-بت: TL و TH.",
    "technicalDeepDive": "The 8051 microcontroller incorporates two 16-bit timers: Timer 0 and Timer 1. Because the architecture is 8-bit, these timers are accessed as two distinct 8-bit registers: TL (Low byte) and TH (High byte). For Timer 0, these are TL0 and TH0, and for Timer 1, they are TL1 and TH1. The timers can be controlled via software or hardware. Using software, they are started and stopped using the TR (Timer Run) bits: TR0 and TR1. They function as timers by counting internal machine cycles, eliminating the need to waste CPU cycles with empty `for` loops (software delays), although real-world C execution might still introduce minor timing discrepancies due to loop overhead.",
    "profFocus": "الدكتور أشار بوضوح: 'كنا بنستخدم الديلاي كود كنا بنحط فور من غير أي حاجة خالص بحيث نضيع وقت لكن التايمر بيقسم الوقت بشكل سايس... ولكن في اختلاف شوية عن التايم الحقيقي لو برمجت بـ C بسبب تنفيذ الأوامر'. وركز إن اللي بينطبق على 8051 بينطبق على 8052 باستثناء عدد المؤقتات.",
    "mermaidCode": "graph LR\n    A[8051 Microcontroller] --> B[Timer 0]\n    A --> C[Timer 1]\n    B --> B1[TL0: 8-bit Low]\n    B --> B2[TH0: 8-bit High]\n    C --> C1[TL1: 8-bit Low]\n    C --> C2[TH1: 8-bit High]\n    B1 --> D[16-bit Timer 0 Register]\n    B2 --> D\n    C1 --> E[16-bit Timer 1 Register]\n    C2 --> E"
  },
  {
    "title": "مسجل ضبط النمط TMOD (Timer Mode Register)",
    "conceptOverview": "مسجل TMOD هو مسجل بحجم 8-بت يُستخدم لتهيئة وتحديد نمط عمل المؤقتات. مقسم إلى جزئين: الـ 4 بتات الأولى (اليمين) لـ Timer 0، والـ 4 بتات العليا (اليسار) لـ Timer 1. يحتوي كل جزء على بتات M0 و M1 لتحديد النمط، وبت C/T لتحديد ما إذا كان سيعمل كمؤقت (مع الكلوك الداخلي) أم كعداد (نبضات خارجية)، وبت GATE لتحديد طريقة التشغيل (سوفت وير أو هارد وير).",
    "technicalDeepDive": "TMOD is an 8-bit configuration register. The lower nibble configures Timer 0, and the upper nibble configures Timer 1. The bit configuration for each nibble is [GATE, C/T, M1, M0]. \n- **M1, M0**: Mode selection.\n  - `00`: Mode 0 (13-bit mode, compatible with 8048. Uses 8 bits of TH and 5 bits of TL).\n  - `01`: Mode 1 (16-bit timer).\n  - `10`: Mode 2 (8-bit auto-reload).\n  - `11`: Mode 3 (Split timer mode).\n- **C/T (Counter/Timer)**: If 0, it operates as a timer (counts machine cycles, internal clock / 12). If 1, it operates as a counter getting pulses from pins T0 (P3.4) or T1 (P3.5).\n- **GATE**: If 0, the timer is started entirely via software by setting the TR bit. If 1, the timer starts only if both TR=1 AND the external interrupt pin (INT0 or INT1) is HIGH. This hardware masking is useful for pulse width measurement.",
    "profFocus": "تحذير من الدكتور: 'لو انا عارف الاربعة بتوع تايمر زيرو هم نفسهم بتوع تايمر وان'. وشدد على إن Mode 0 (13-bit) مش بنستخدمه كتير دلوقتي عشان هو معمول للتوافق مع الأنظمة القديمة زي 8048. التركيز الأساسي في المسائل على Mode 1 و Mode 2.",
    "mermaidCode": "classDiagram\n    class TMOD_Register {\n        +Upper 4-bits: Timer 1\n        +Lower 4-bits: Timer 0\n        +GATE: Hardware/Software Control\n        +C/T: Counter or Timer Select\n        +M1: Mode bit 1\n        +M0: Mode bit 0\n    }\n    note for TMOD_Register \"Timer 1: [GATE][C/T][M1][M0] | Timer 0: [GATE][C/T][M1][M0]\""
  },
  {
    "title": "تشغيل نمط 16-بت (Mode 1 Programming) والحسابات الزمنية",
    "conceptOverview": "في Mode 1، بيشتغل المؤقت بـ 16-بت، يعني بيعد من 0000 حتى FFFF. لما بيوصل لأقصى قيمة (FFFF) ويرجع للصفر بيعمل ريسيت ويقوم برفع علم يسمى TF (Timer Flag) لـ 1. الكلوك الداخلي بيدخل على بوابة AND (ماسكينج) اللي لازم يكون فيها الـ TR بواحد عشان الكلوك تعدي. لحساب القيمة الابتدائية: بنحسب الزمن المطلوب وبنقسمه على زمن الـ Machine Cycle لنجيب عدد العدادات، وبنطرحه من 65536 عشان نجيب القيمة اللي هنحطها في TL و TH.",
    "technicalDeepDive": "In Mode 1, the timer acts as a 16-bit counter using TH and TL. The machine cycle clock (Crystal Frequency / 12) is fed into an AND gate. The other input of the AND gate is the TR bit (Timer Run). Setting TR=1 allows the clock pulses to pass through ('Masking'). The 16-bit register increments up to FFFFH (65535). Upon rolling over to 0000H, the TF (Timer Flag) bit is set to 1. \n\n**Mathematical Proof for Timer Delay Calculation:**\n1. Number of Ticks required = Delay Time / Machine Cycle Time.\n2. Machine Cycle = (1 / Crystal Freq) * 12. For an 11.0592 MHz crystal, Machine Cycle = 1.085 µs.\n3. Since the timer counts UP, the initial value to be loaded is: `Initial Value = 65536 - Number of Ticks`.\n4. Convert the Initial Value to Hexadecimal. The upper byte goes into THx and the lower byte goes into TLx.\nIf the required delay exceeds the maximum capability of a 16-bit timer (which is 65536 * 1.085 µs ≈ 71 ms), the programmer must loop the timer multiple times. E.g., for a 500 ms delay, create a 25 ms timer delay and enclose it in a `for` loop executing 20 times. However, loop execution in C inherently consumes extra clock cycles, making this slightly imprecise in real-world C execution vs theoretical calculation.",
    "profFocus": "توضيح رياضي مهم من الدكتور: 'لما يوصل لأخر رقم ويرجع للزيرو محتاج كلوك زيادة كمان عشان كدا بنطرح من 65536... بنطرح 65536 ناقص العدد اللي احنا عايزينه، الرقم اللي هيطلع ده اللي هنحطه في الكاونتر كـ Initial Value'. ونبه بشدة إن لو الزمن المطلوب كبير جداً، بنعمل لوب، و'عشان الفول لوب بياخد وقت أساسا هيطلع وقت أزيد... عشان كدا في العملي مستحيل تطلع بالسايز بالضبط'.",
    "mermaidCode": "graph TD\n    A[Oscillator freq / 12] --> B(AND Gate)\n    C[TR Bit] --> B\n    B -->|Clock Pulses| D[TL Register: 8-bit]\n    D -->|Overflow| E[TH Register: 8-bit]\n    E -->|Overflow| F((TF Flag set to 1))\n    style B fill:#f9f,stroke:#333,stroke-width:2px"
  },
  {
    "title": "خطوات البرمجة لـ Mode 1 (Algorithm of Timer Mode 1)",
    "conceptOverview": "لبرمجة التايمر في Mode 1، عندنا خطوات ثابتة: 1- نحط القيمة المناسبة في TMOD. 2- نحط القيمة الابتدائية في مسجلات TL و TH. 3- نشغل التايمر بتفعيل TR. 4- نراقب الفلاج TF في لوب while(TF==0). 5- بمجرد ما الـ TF يبقى 1، بنوقف التايمر (TR=0) وبنعمل كلير للفلاج (TF=0) عشان الدورة الجاية، ونرجع نعيد.",
    "technicalDeepDive": "The algorithm for generating a time delay in Mode 1 consists of deterministic steps:\n1. **Load TMOD**: Write to the TMOD register to configure the timer mode (e.g., `TMOD = 0x01` for Timer 0, Mode 1).\n2. **Load Initial Count**: Write the calculated initial values into `THx` and `TLx`.\n3. **Start Timer**: Set `TRx = 1`.\n4. **Monitor Flag**: Block execution using a loop like `while (TFx == 0);`. The timer increments automatically in the background.\n5. **Stop Timer**: Once `TFx == 1`, execution exits the loop. Stop the timer by clearing the run bit `TRx = 0`.\n6. **Clear Flag**: Manually clear the timer flag `TFx = 0` to ready the system for the next cycle.\n7. **Repeat**: Jump back to step 2 for continuous operation.",
    "profFocus": "الدكتور أكد على إن: 'كل العملية دي بتاعة العد بتبقى اوتوماتيك لكن القصة دي لازم تعتبر كأنك بتتابعها... وايل تي اف بتساوي زيرو معناها تفضل منتظر لغاية ما يخلص'. ونبه على ضرورة عمل Clear لـ TR و TF بعد انتهاء العد.",
    "mermaidCode": "flowchart TD\n    S1[1. Load TMOD Register] --> S2[2. Load Initial values to TH and TL]\n    S2 --> S3[3. Start Timer: TR = 1]\n    S3 --> S4{4. Is TF = 1?}\n    S4 -- NO --> S4\n    S4 -- YES --> S5[5. Stop Timer: TR = 0]\n    S5 --> S6[6. Clear Flag: TF = 0]\n    S6 --> S7[7. Repeat if needed]"
  },
  {
    "title": "نمط إعادة التحميل التلقائي 8-بت (Mode 2 Auto-Reload Programming)",
    "conceptOverview": "في Mode 2 التايمر بيشتغل بـ 8-بت فقط، وده بيحل مشكلة إننا كل مرة نضطر نحط القيم الابتدائية من جديد. هنا بنحط القيمة الابتدائية في مسجل TH، والمايكروكنترولر بياخد منها نسخة تلقائيا ويحطها في TL. الـ TL هو اللي بيعد، ولما يوصل لأقصى قيمة (FFH) وينزل للصفر، بيعمل ريسيت وبياخد النسخة اللي في TH يحطها تاني في TL لوحده! ده مناسب جدا للترددات العالية والسرعات الكبيرة.",
    "technicalDeepDive": "Mode 2 configures the timer as an 8-bit auto-reload timer. The 16-bit register is functionally split. The programmer loads the initial 8-bit value into the TH register. The hardware automatically copies this value into the TL register. The TL register increments with every machine cycle. Upon rolling over from FFH (255) to 00H, the TF flag is set to 1, and the hardware instantly reloads the TL register with the value stored in the TH register, without software intervention. \n\n**Mathematical Execution for Mode 2:**\n1. Max count is 255.\n2. Ticks = `256 - Initial Value`.\n3. Since 8-bit math is straightforward, instead of calculating `256 - ticks`, you can directly load `-ticks` into TH. The C compiler automatically calculates the 2's complement. For instance, to generate a 2500 Hz square wave:\n   - Frequency = 2500 Hz -> Period = 400 µs.\n   - Half Period (Toggle duration) = 200 µs.\n   - At 1 µs per machine cycle, Ticks = 200.\n   - `TH = -200;` (This evaluates to `0x38` or `56`).",
    "profFocus": "ملاحظة مهمة من الدكتور: 'هنا الموضوع أسهل بكتير بدل ما تقعد تحسب، ما إنت ممكن تحط الرقم نفسه بالنيجاتيف جوه الكاونتر، هو لوحده هيحسبها... يعني سالب 200 هتتحسب صح'. وأكد على إن Mode 2 أقصى قيمة فيه 255 فبستخدمه مع الأزمنة الصغيرة جداً (بالمايكروثانية).",
    "mermaidCode": "graph LR\n    A[Internal Clock] --> B[TR Bit AND Gate]\n    B --> C[TL: 8-bit Counter]\n    C -->|Overflow from FFH| D((TF Flag Set))\n    E[TH: 8-bit Reload Value] -.->|Auto-Reload on Overflow| C\n    style E fill:#ccf,stroke:#333,stroke-width:2px"
  },
  {
    "title": "برمجة العدادات (Counter Programming)",
    "conceptOverview": "الـ Counter هو استخدام التايمر لعد أحداث أو نبضات خارجية بدل ما يعد الوقت الداخلي. بنخلي بت C/T في TMOD بواحد (C/T=1). في الحالة دي الكلوك بتيجي من برة المايكروكنترولر عن طريق أطراف مخصصة: T0 لتايمر 0 و T1 لتايمر 1. هنا مفيش حسابات للماشين سايكل لأننا بنعد النبضات اللي بتيجي من برة بشكل مباشر، يعني كل نبضة (من 1 لـ 0) بتزود العداد.",
    "technicalDeepDive": "When the C/T bit in the TMOD register is set to 1, the timer operates as a Counter. Instead of deriving its clock from the oscillator frequency divided by 12, it looks for external pulses. The pulses are fed through physical pins: Pin P3.4 for T0 (Counter 0) and Pin P3.5 for T1 (Counter 1). The counter increments on the falling edge (1-to-0 transition) of the external signal. This removes the dependency on the machine cycle duration for counting logic. It's often used with Mode 2 or Mode 1. In typical code, the TL register holds the count which can be outputted to an external port continuously. For example, if we want to monitor Counter 1 in Mode 2, we set `TMOD = 0x60` (which is `0110 0000` -> Timer 1, Mode 2, C/T=1).",
    "profFocus": "الدكتور وضح: 'إيه الفرق بينهم؟ الكاونتر بياخد من البنا الخارجية... لكن التايمر بيحسب على الماشين سايكل. عشان كدا الكود بتاع الكاونتر أسهل لأننا مبنحسبش ماشين سايكل، إحنا بنعد حاجة خارجية'. وأشار لضرورة الانتباه لـ Bouncing Effect في السويتشات الميكانيكية لأنها بتزود العداد بالغلط.",
    "mermaidCode": "graph TD\n    A[External Pin T0 or T1] --> B{Falling Edge Detector}\n    B -->|Pulse| C[TL Register: 8-bit/16-bit]\n    C -->|Overflow| D((TF Flag))\n    C -->|Current Count| E[Output Port e.g., P1]"
  },
  {
    "title": "التحكم عن طريق الهاردوير (GATE Control)",
    "conceptOverview": "بت GATE في TMOD بيحدد مين اللي بيتحكم في تشغيل المؤقت. لو الـ GATE بصفر، السوفت وير هو اللي متحكم بشكل كامل عن طريق TR. لكن لو الـ GATE بواحد، لازم الإشارة الخارجية على طرف الانترابت (INT0 لتايمر 0 أو INT1 لتايمر 1) تكون HIGH، وكمان السوفت وير (TR) يكون HIGH عشان التايمر يشتغل. ده بيستخدم لقياس عرض النبضات (Pulse Width).",
    "technicalDeepDive": "The GATE bit introduces hardware constraint onto the timer running mechanism. \n- **GATE = 0**: The enabling condition for the timer clock passes purely through an OR gate (where the interrupt pin is ignored) and then to the AND gate controlled by TR. Ergo, if `TR=1`, the timer runs.\n- **GATE = 1**: The external interrupt pin (INT0 for Timer 0, INT1 for Timer 1) directly acts as an enable line. The timer will only run if `TRx = 1` AND the external `INTx` pin is HIGH. This logic is commonly employed to precisely measure the width of an external incoming pulse without latency-prone polling logic.",
    "profFocus": "سؤال الدكتور: 'لو خليت الجيت بواحد يبقى الأوتبوت معتمد على مين؟ على الانترابت... يبقى إنت عشان التايمر يشتغل لازم TR بواحد وكمان الانترابت بواحد، مستني الانترابت'.",
    "mermaidCode": "graph LR\n    A[INT Pin] --> B(OR Gate)\n    C[GATE bit inverted] --> B\n    B --> D(AND Gate)\n    E[TR Bit] --> D\n    D -->|Enable| F[Timer Counting Logic]\n    style D fill:#f96,stroke:#333,stroke-width:2px"
  }
];